import { HMSUpdateListener } from '../../interfaces';
import { IStore } from '../../sdk/store';
import HMSTransport from '../../transport';
export declare class WhiteboardManager {
    private store;
    private transport;
    listener?: HMSUpdateListener | undefined;
    constructor(store: IStore, transport: HMSTransport, listener?: HMSUpdateListener | undefined);
    handleNotification(method: string, notification: any): void;
    private handleWhiteboardUpdate;
}
