<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Pharmacy Signup</title>

  <!-- Bootstrap 5 CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >

  <style>
    /* Background gradient matching the MediCare Plus theme */
    body {
      background: linear-gradient(135deg, #e6f0fa 0%, #cde0f7 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Segoe UI", Tahoma, sans-serif;
    }

    .signup-card {
      background-color: #ffffffcc; /* subtle transparency */
      border: none;
      border-radius: 1.2rem;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .signup-header {
      background: #1d6ae5; /* deep blue header */
      color: #fff;
      border-top-left-radius: 1.2rem;
      border-top-right-radius: 1.2rem;
      text-align: center;
      padding: 1.5rem;
    }

    .btn-primary {
      background-color: #1d6ae5;
      border-color: #1d6ae5;
    }
    .btn-primary:hover {
      background-color: #1554b8;
      border-color: #1554b8;
    }

    label {
      font-weight: 500;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-6 col-md-8">
        <div class="card signup-card">
          <div class="signup-header">
            <h3 class="mb-0">Pharmacy Signup</h3>
          </div>
          <div class="card-body p-4">
            <form action="/pharmacy/signup" method="POST" enctype="multipart/form-data">
              
              <!-- Pharmacy License Number -->
              <div class="mb-3">
                <label for="pharmacyLicense" class="form-label">Pharmacy License Number</label>
                <input type="text"
                       class="form-control"
                       id="pharmacyLicense"
                       name="pharmacyLicense"
                       placeholder="Enter your pharmacy license number"
                       required>
              </div>

              <!-- Upload Pharmacy License Photocopy -->
              <div class="mb-4">
                <label for="licensePhoto" class="form-label">Upload Pharmacy License Photocopy</label>
                <input class="form-control"
                       type="file"
                       id="licensePhoto"
                       name="licensePhoto"
                       accept="image/*,application/pdf"
                       required>
              </div>



              <!-- Submit -->
              <button type="submit" class="btn btn-primary w-100 py-2">Sign Up</button>

            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
